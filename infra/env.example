# 4Runr Gateway Environment Configuration
# Copy this file to infra/.env and update with your values

# Server configuration
NODE_ENV=development
PORT=3000
HOST=127.0.0.1

# Database (PostgreSQL)
DATABASE_URL=postgresql://4runr:4runr_password@postgres:5432/4runr_gateway

# Redis (optional in development)
# REDIS_URL=redis://redis:6379

# Security (generate secure keys for production)
JWT_SECRET=your-super-secret-jwt-key-minimum-32-characters-long
ENCRYPTION_KEY=your-super-secret-encryption-key-minimum-32-characters-long

# Sentinel configuration
SENTINEL_STORE_PLAIN=true
SENTINEL_MODE=live
SENTINEL_SHIELD_MODE=enforce

# Logging
LOG_LEVEL=info

# Development
DEV_MODE=true
ALLOW_LOCALHOST_ONLY=true

# =============================================================================
# PRODUCTION SETTINGS (uncomment and configure for production)
# =============================================================================

# NODE_ENV=production
# PORT=3000
# HOST=0.0.0.0

# # Use external database in production
# DATABASE_URL=postgresql://username:password@your-db-host:5432/4runr_gateway

# # Use external Redis in production
# REDIS_URL=redis://your-redis-host:6379

# # Generate secure keys for production
# JWT_SECRET=generate-a-secure-random-string-at-least-32-characters-long
# ENCRYPTION_KEY=generate-a-secure-random-string-at-least-32-characters-long

# # Disable plain text storage in production
# SENTINEL_STORE_PLAIN=false

# # Enable strict mode in production
# SENTINEL_MODE=live
# SENTINEL_SHIELD_MODE=enforce

# # Production logging
# LOG_LEVEL=warn

# # Disable development features
# DEV_MODE=false
# ALLOW_LOCALHOST_ONLY=false
